{
  "name": "@corates/workers",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Workers with Durable Objects",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest",
    "clear-workers": "rm -rf .wrangler/state",
    "db:migrate": "wrangler d1 migrations apply corates-db --local",
    "db:migrate:prod": "y | wrangler d1 migrations apply corates-db"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20251121.0",
    "better-auth": "^1.4.1",
    "drizzle-orm": "^0.44.7",
    "yjs": "^13.6.27"
  },
  "devDependencies": {
    "vitest": "^4.0.13",
    "wrangler": "^4.50.0"
  }
}
