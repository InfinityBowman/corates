apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    # Container-to-container over the shared homelab network; no auth needed internally
    url: http://corates-loki:3100
    isDefault: true
