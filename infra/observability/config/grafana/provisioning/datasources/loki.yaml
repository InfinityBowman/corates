apiVersion: 1

datasources:
  - name: Loki
    type: loki
    # Pinned because alert rules reference it by uid
    uid: P8E80F9AEF21F6940
    access: proxy
    # Container-to-container over the shared homelab network; no auth needed internally
    url: http://corates-loki:3100
    isDefault: true
